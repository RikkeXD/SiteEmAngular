export interface Pessoa {
    id?: number;
    nome: string;
    sobrenome: string;
    dtNascimento: string;
}

export interface Produto {
    id?: number;
    nome: string;
    descricao: string;
    quantidade: number;
}